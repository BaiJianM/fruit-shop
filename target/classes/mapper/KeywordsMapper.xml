<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyuyouguo.mapper.KeywordsMapper">

    <sql id="Base_Column_List">
        ${alias}.id,
        ${alias}.keyword,
        ${alias}.is_hot,
        ${alias}.is_default,
        ${alias}.is_show,
        ${alias}.sort_order,
        ${alias}.scheme _url,
        ${alias}.type
    </sql>

</mapper>