<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyuyouguo.mapper.GoodsMapper">

    <sql id="Base_Column_List">
        ${alias}.id,
        ${alias}.category_id,
        ${alias}.is_on_sale,
        ${alias}.name,
        ${alias}.goods_number,
        ${alias}.sell_volume,
        ${alias}.keywords,
        ${alias}.retail_price,
        ${alias}.min_retail_price,
        ${alias}.cost_price,
        ${alias}.min_cost_price,
        ${alias}.goods_brief,
        ${alias}.goods_desc,
        ${alias}.sort_order,
        ${alias}.is_index,
        ${alias}.is_new,
        ${alias}.goods_unit,
        ${alias}.https_pic_url,
        ${alias}.list_pic_url,
        ${alias}.freight_template_id,
        ${alias}.freight_type,
        ${alias}.is_delete,
        ${alias}.has_gallery,
        ${alias}.has_done
    </sql>

</mapper>
