<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyuyouguo.mapper.ProductMapper">

    <sql id="Base_Column_List">
        ${alias}.id,
        ${alias}.goods_id,
        ${alias}.goods_specification_ids,
        ${alias}.goods_sn,
        ${alias}.goods_number,
        ${alias}.retail_price,
        ${alias}.cost,
        ${alias}.goods_weight,
        ${alias}.has_change,
        ${alias}.goods_name,
        ${alias}.is_on_sale,
        ${alias}.is_delete
    </sql>

</mapper>