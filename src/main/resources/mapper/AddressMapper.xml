<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyuyouguo.mapper.AddressMapper">

    <sql id="Base_Column_List">
        ${alias}.id,
        ${alias}.name,
        ${alias}.user_id,
        ${alias}.country_id,
        ${alias}.province_id,
        ${alias}.district_id,
        ${alias}.address,
        ${alias}.mobile,
        ${alias}.is_default,
        ${alias}.is_delete
    </sql>

</mapper>
