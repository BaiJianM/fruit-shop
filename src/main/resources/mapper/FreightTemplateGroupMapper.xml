<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyuyouguo.mapper.FreightTemplateGroupMapper">

    <sql id="Base_Column_List">
        ${alias}.id,
        ${alias}.template_id,
        ${alias}.is_default,
        ${alias}.area,
        ${alias}.start,
        ${alias}.start_fee,
        ${alias}.add,
        ${alias}.add_fee,
        ${alias}.free_by_number,
        ${alias}.free_by_money,
        ${alias}.is_delete
    </sql>

</mapper>