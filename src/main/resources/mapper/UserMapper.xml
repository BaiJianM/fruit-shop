<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyuyouguo.mapper.UserMapper">

    <sql id="Base_Column_List">
        ${alias}.id,
        ${alias}.nickname,
        ${alias}.name,
        ${alias}.username,
        ${alias}.password,
        ${alias}.gender,
        ${alias}.birthday,
        ${alias}.register_time,
        ${alias}.last_login_time,
        ${alias}.last_login_ip,
        ${alias}.mobile,
        ${alias}.register_ip,
        ${alias}.avatar,
        ${alias}.weixin_openid,
        ${alias}.name_mobile,
        ${alias}.country,
        ${alias}.province,
        ${alias}.city
    </sql>

</mapper>
