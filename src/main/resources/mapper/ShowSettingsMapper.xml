<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyuyouguo.mapper.ShowSettingsMapper">

    <sql id="Base_Column_List">
        ${alias}.id,
        ${alias}.banner,
        ${alias}.channel,
        ${alias}.index_banner_img,
        ${alias}.notice
    </sql>

</mapper>
